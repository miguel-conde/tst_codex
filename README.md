# tst_codex

This repository contains a simple Python script to plot a cosine wave.

## Running the example

Install the required dependencies and run the script:

```bash
pip install matplotlib numpy
python plot_cosine.py
```
